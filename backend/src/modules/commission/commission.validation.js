// Phase 9B: approve and pay endpoints have no request body —
// only req.params.id and req.user.id are used.
// List endpoint query params are optional strings handled directly
// in the service (consistent with commissionRule.service.js pattern).
// No Valibot middleware schemas are required for this module.
